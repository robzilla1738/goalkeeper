# Python
__pycache__/
*.py[cod]

# Goalkeeper project-local state (created at runtime in target repos)
.goalkeeper/
# ...but the committed example contracts under examples/ are intentional.
!examples/**/.goalkeeper/
!examples/**/.goalkeeper/state.json
